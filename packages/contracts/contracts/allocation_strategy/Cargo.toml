[package]
name = "allocation-strategy-contract"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }
nester-common = { path = "../../libs/common" }
nester-access-control = { path = "../access_control" }
# Cross-contract call to validate yield sources during set_weights.
yield-registry-contract = { path = "../yield_registry" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
nester-test-utils = { path = "../../libs/test_utils" }
yield-registry-contract = { path = "../yield_registry" }
